use super::*;

pub fn ui_wait_player(
    players: Vec<(String, usize, bool)>,
    msg: Vec<String>, roomid: String,
){
}

pub fn ui_wait_ready(
    players: Vec<(String, usize, bool)>,
    msg: Vec<String>, roomid: String,
) {

}
